---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: nodegroups.kube-shuffle-sharder.io
spec:
  group: kube-shuffle-sharder.io
  names:
    kind: NodeGroups
    listKind: NodeGroupsList
    plural: nodegroups
    singular: nodegroups
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: NodeGroups is the Schema for the nodegroups API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NodeGroupsSpec defines the desired state of NodeGroups
            type: object
          status:
            description: NodeGroupsStatus defines the observed state of NodeGroups
            properties:
              nodeGroups:
                additionalProperties:
                  type: integer
                description: NodeGroups is a map of node groups & the number of nodes
                  contained in each group
                type: object
            required:
            - nodeGroups
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
